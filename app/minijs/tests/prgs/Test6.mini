
: (:)
main = -> {
  f();
  return;
  return x + 1;
  {
    val a = 2;
    Mc.print(a);
  }

  while (x == 4) {
    a--; m();
    ++a; m();
    x--
  }

  var text = "";
  var i = 0;
  do {
    text += "The number is " + i;
    i++;
  }
  while (i < 5);

  for (x = 5, 3, -1) { // for
    val a = 2;
    Mc.print(a);
  }

  for (x = 3, 8) { // for
    val a = 2;
    Mc.print(a);
  }

  for (i, 3, 2) { // for0
    val a = 2;
    Mc.print(a);
  }

  for (i, 3) { // for0
  }

  for (x :: array) { // for_each
  }

  if (x == 5) {
    return a
  }

  if (x == 5) {
    return a
  } elif (x < 2) {
    b()
  } else {
    c()
  }

  if (x != 5) {
    return a
  } else {
    b()
  }

  if (x == 5) {
    return a
  } elif (x < 2) {
    b()
  }

  var t;
  try {
    t = x / y;
  } catch (e) {
    console.log("error" + e)
  } finally {
    t = x * x;
  }

  try {
    t = x / y;
    throw (new Exc("Oh!"))
  } catch (e) {
    console.log("error" + e)
  }

  var x = 0;
  native NATIVE
    x = x + 1;

    x = x + 2;
  NATIVE;
  console.log(c);

}
